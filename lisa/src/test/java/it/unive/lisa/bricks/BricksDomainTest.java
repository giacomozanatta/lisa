package it.unive.lisa.bricks;

import it.unive.lisa.imp.ParsingException;
import org.junit.Test;

import it.unive.lisa.AnalysisException;
import it.unive.lisa.LiSA;


public class BricksDomainTest {

    @Test
    public void testBricks() throws ParsingException, AnalysisException {
       /* LiSA lisa = new LiSA();
        lisa.setDumpAnalysis(true);
        lisa.setWorkdir("test-outputs/sign-domain");
        lisa.setProgram(IMPFrontend.processFile("signs.imp"));
        lisa.setAbstractState(LiSAFactory.getDefaultFor(AbstractState.class,
                LiSAFactory.getDefaultFor(HeapDomain.class), new BricksDomain()));
        lisa.run();*/
    }
}